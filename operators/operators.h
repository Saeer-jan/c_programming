/*
operators:
these are used to perform operations on variables and values. C programming provides various 
types of operators such as arithmetic, logical, bitwise, relational, assignment, and conditional 
operators.

in short operators are used in operations on operands.
so operators are symbols that tell the compiler to perform specific mathematical or logical
operations.
a = 1 + 2; // here + is an operator and a is an operand

types of operators:
uniray operators: these are operators that operate on a single operand.
for example, the unary minus operator (-) negates a number.
for example, -x is the unary minus operator that negates the value of x. and x is the operand.let's 
5 say x = 5, then -x = -5.
=> unary operators are operators that operate on a single operand.
=> increment operator (++) increases the value of a variable by 1.
=> decrement operator (--) decreases the value of a variable by 1.
=> logical NOT operator (!) negates the value of a boolean expression.for example, if x is true,
 then !x is false.let's say x = 1, then !x = 0.


3. ternary operators: these are operators that operate on three operands.

*/