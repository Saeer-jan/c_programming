{
  "name": "cogniflow",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w client & npm run dev -w server",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "lint": "npm run lint -w client || true"
  },
  "engines": {
    "node": ">=18.18.0",
    "npm": ">=9"
  }
}
